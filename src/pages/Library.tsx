import React from "react";
import { Text } from "react-native";

const Library: React.FC<{}> = () => {

    return (
        <Text>
            Library
        </Text>
    );
}

export default Library;